export const environment = {
  production: false,
  
  // Apunta a la ruta relativa que Nginx interceptará
  apiUrl: '/api',
  
  // La URL del frontend es la que sirve Nginx
  frontendUrl: 'http://localhost'
};